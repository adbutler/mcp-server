{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "AdButler MCP Configuration",
  "description": "Configuration for connecting to the AdButler MCP server.",
  "properties": {
    "apiKey": {
      "type": "string",
      "title": "AdButler API Key",
      "description": "Your AdButler API key. Get it from AdButler Dashboard → Settings → API Keys.",
      "format": "password",
      "x-to": { "header": "Authorization" }
    }
  },
  "required": ["apiKey"]
}
